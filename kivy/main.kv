<container>:
    display : display
    rows: 1
    BoxLayout:
        orientation: vertical
        add:
            return on_release : root.add_one()
        subtract:
            return on_release : root.subtract_one()
    Label:
        id : display
        font_size : dp(50)
        text : ‘0’
